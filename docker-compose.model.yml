services:
  worker:
    environment:
      XIAOWEI_GEMINI_ENABLED: "true"
    secrets:
      - postgres_password
      - gemini_api_key

secrets:
  gemini_api_key:
    file: ./.secrets/gemini_api_key
